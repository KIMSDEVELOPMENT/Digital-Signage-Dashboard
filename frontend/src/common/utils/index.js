export const BRANCH_LOCATIONS = {
  PBMH: ['A Block', 'B/C Block'],
  SSCC: ['KSS', 'KCC'],
  Dental: ['Dental'],
};

export const BRANCHES = Object.keys(BRANCH_LOCATIONS);

export const ALL_LOCATIONS = Object.values(BRANCH_LOCATIONS).flat();
